version: '3.8'

services:
  batteryqk:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Add your env vars here or mount a .env file
    volumes:
      - .:/usr/src/app
    restart: unless-stopped
